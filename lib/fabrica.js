
 * de Firebase.
*   @returns {
      importar ("./ tiposFire").
      Firestore} * /
 función de  exportación getFirestore ( )  {
  // @ts -ignore
  volver base de  fuego . firestore ( ) ;
}

/ ** Conexión al sistema de
 * autenticación de Firebase.
*   @returns {
      importar ("./ tiposFire").
      Auth} * /
 función de  exportación getAuth ( )  {
  // @ts -ignore
  volver base de  fuego . auth ( ) ;
}

/ ** Conexión al sistema de
 * almacenamiento de Firebase.
*   @returns {
      importar ("./ tiposFire").
      Almacenamiento} */
 función de  exportación getStorage ( )  {
  // @ts -ignore
  volver base de  fuego . almacenamiento ( ) ;
}
